public class O310Linked_List {
    public static void main(String[] args) {
        //Array is called a continuous memory allocation
        //But internally in java array is not continuous
        //However we call it continuous

        //Limitations of array
        //Size is constant
        //ArrayList is made for tackling this issue
        //But still its internal implementation involves creating array of double size when default size array is filled and then copying the elements in it

        //Hence Linked List is made to handle this
        //It is a non-continuous data structure
        //Separate memory spaces in heap points to next block
        //Singly Linked list is like one-sided love

        //Reference variables in linked list are head and tail
        //head points to first node
        //tail points to last node in a linked list
    }
}
