public class O384Leetcode_84_Largest_Rectangle_In_Histogram {
    //For each bar move towards the right till the element is smaller or end is reached
    //Similarly for each element move towards the left to till the element smaller than the current bar or index reaches less than 0
    //Use stack to find the next smaller element
}
